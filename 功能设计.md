好的，明白。既然你需要一份**纯粹从产品/业务操作视角出发的完整功能文档**（不含任何技术实现代码、数据库字段或架构图），我为你重新编写了以下《产品功能规格说明书（用户操作手册）》。

这份文档可直接交付给产品经理、UI设计师或业务方进行评审，明确**用户能看到什么页面、操作什么按钮、系统如何反馈**。

---

# 房地产 AI 销售助手平台——产品功能规格说明书（用户操作手册）

| 文档版本 | V1.0（产品版） |
| :--- | :--- |
| 发布日期 | 2026-09 |
| 适用对象 | 产品经理、UI设计、业务验收方、终端用户培训 |
| 核心原则 | **AI辅助，人工兜底**：所有AI生成内容均需经人工确认或复核，系统明确提示“仅供参考”。 |

---

## 一、产品概述与用户角色

### 1.1 产品定位
专为房地产中介、楼盘策划、营销部门打造的**私有化AI生产力工具**。它不是一个冰冷的自动机器，而是一个**懂房产的协作文档助手**。

### 1.2 用户角色与权限概览
| 角色 | 权限范围 | 典型职责 |
| :--- | :--- | :--- |
| **普通成员** | 个人对话、上传文件、调用Skill、查看自己所在部门的数据 | 日常创作文案、解析户型图 |
| **管理员** | 全部普通权限 + 知识库管理 + Skill安装/卸载 + 查看全部门对话审计 | 运营维护、数据喂养、权限分配 |

---

## 二、全局导航与通用交互规范

### 2.1 页面布局（Web端）
- **顶部导航栏**：左侧Logo/产品名，中间全局搜索框（搜索历史对话/文档），右侧显示【任务中心】铃铛图标 + 当前用户名/头像下拉菜单（含“系统设置”与“退出登录”）。
- **左侧边栏（主工作台）**：显示“新建对话”按钮及历史会话列表。
- **主内容区**：根据菜单切换（对话/户型工具/知识库/Skill市场等）。

### 2.2 通用按钮与反馈规范
- **加载状态**：任何按钮点击后，若响应超过0.5秒，按钮变为“加载中...”禁用态，防止重复点击。
- **成功/失败提示**：所有操作结果统一在页面右上角弹出轻提示（Toast）：
  - ✅ **绿色**：“保存成功”、“任务已下发”。
  - ⚠️ **橙色**：“请检查必填项”、“知识库暂无相关内容”。
  - ❌ **红色**：“网络超时，请重试（错误码：1005）”。
- **AI生成内容标识**：所有由AI生成的文本、图片、方案，**左上角必须带有一个浅灰色“AI生成”水印/标签**，并附小字“仅供参考，请专业复核”。

---

## 三、核心功能模块详解

### 模块 3.1：智能对话工作台（核心高频操作区）

这是业务人员每天打开电脑后第一个面对的界面。

- **3.1.1 新建与切换会话**
  - **操作**：点击左侧边栏顶部【➕ 新建对话】按钮，立即在主区域生成空白对话窗口。
  - **历史查看**：左侧边栏按“今天/昨天/更早”分组展示历史会话。鼠标悬停时，右侧出现【重命名】铅笔图标和【删除】垃圾桶图标。
  - **断点续聊**：点击任何历史会话，界面完全恢复至上次对话的最后一条消息状态，包含当时上传的图片附件。

- **3.1.2 消息输入区（重点交互）**
  - **输入框**：位于页面底部，多行文本框（支持Shift+Enter换行，Enter直接发送）。
  - **快捷指令（@技能）**：在输入框中输入“`@`”符号，系统弹出下拉菜单列出所有已安装的Skill（如“@户型解析”、“@文案生成”）。选择后，该Skill名称变为蓝色标签插入输入框。
  - **附件上传（📎回形针）**：
    - 点击【📎】按钮，弹出系统文件选择器（支持图片、PDF、Word、DXF）。
    - 选择文件后，输入框**上方**生成文件标签卡片（文件名 + 文件大小），右侧带“✕”移除按钮。
    - **多文件支持**：可连续添加多个附件，一并发送。

- **3.1.3 消息流式响应展示（打字机效果）**
  - 发送消息后，AI回复区域顶部出现**动态状态条**（灰色底色），依次变化：
    - “🧠 规划任务中...”
    - “⚙️ 正在调用插件/查询知识库...”
    - “✅ 校验合规中...”
    - “✍️ 生成回答...”
  - 文字内容像真人打字一样逐字输出。回复底部展示**引用来源**折叠卡片，点击展开可查看原文片段（显示文档名称和页码）。

- **3.1.4 对话中的快捷操作（对AI回复的反馈）**
  - 鼠标悬停在AI回复气泡上，出现悬浮按钮组：
    - 【👍 有用】 / 【👎 无用】（用于后台统计准确率）。
    - 【📋 复制】一键复制回复内容。
    - 【🔄 重新生成】对当前问题重新请求一次新的回答（消耗额外计算资源）。

---

### 模块 3.2：户型设计编辑器（核心兜底功能）

当AI解析户型图出现偏差时，业务人员可在此界面直接动手修正，系统实时校验合规性。

- **3.2.1 进入编辑器**
  - **入口A**：在对话窗口直接发送户型图片，AI识别完成后，回复卡片中带有一个【📐 打开户型编辑】按钮。
  - **入口B**：顶部导航栏独立菜单【户型工具】 -> 【新建解析】，上传文件后进入。

- **3.2.2 编辑器界面布局（左右分屏）**
  - **左侧（预览区）**：显示上传的原始户型图片或DXF线稿。支持鼠标滚轮缩放、拖拽平移。
  - **右侧（参数编辑区）**：以表单形式展示AI提取的结构化数据。

- **3.2.3 表单字段与人工修正操作**
  - **基础信息**：户型名称（文本输入）、所属楼盘（下拉选择/模糊搜索）。
  - **数值类（可直接修改）**：建筑面积(㎡)、套内面积(㎡)、房间数量（数字增减器）。
  - **选项类（下拉修改）**：朝向（东/南/西/北）、户型格局（平层/复式/LOFT）。
  - **交互细节**：点击任何一个数值框，可直接键盘输入覆盖AI识别结果。

- **3.2.4 实时合规校验与提交**
  - **校验触发**：每当用户修改任意字段，或点击表单下方的【🔥 校验当前数据】按钮时触发。
  - **校验反馈**：
    - ✅ 通过：字段右侧显示绿色对勾。
    - ❌ 不通过（如房间面积总和大于建筑面积）：对应字段标红，底部显示具体错误说明条。
  - **提交按钮**：
    - 【确认并保存】（仅在无红色错误时可点击）。
    - 【强制忽略并保存】（有红色错误时变亮，点击弹窗二次确认“数据不合规，确定提交吗？”，用于特殊紧急情况）。

---

### 模块 3.3：知识库管理后台（管理员数据喂养）

运营/管理员用来给AI投喂业务资料（楼盘楼书、政策法规、历史文案）。

- **3.3.1 知识库总览列表**
  - **视图**：表格形式，列示：文档名称、类型、文件大小、上传人、状态（生效中/处理中/已失效）、版本号。
  - **操作列**：【查看详情】（跳转至文档切片预览页） / 【停用】（紧急下架问题文档）。

- **3.3.2 上传与版本冲突处理**
  - **操作**：点击【📤 上传文档】按钮，弹出大尺寸拖拽弹窗。
  - **批量上传**：弹窗内支持框选多份文件或直接Ctrl+A全选。
  - **版本冲突弹窗（关键交互）**：
    - 当系统检测到上传的文件名与已有文档重名时，**不会静默覆盖**，而是弹出明确弹窗让用户选择：
      - **选项A**：“作为新版本上传（旧版本自动失效归档）”
      - **选项B**：“作为独立新文档保留（两个版本并存）”

- **3.3.3 知识库检索测试（查漏补缺）**
  - **入口**：知识库页面顶部的【🔍 试搜一下】搜索框。
  - **操作**：输入业务自然语言问题（如“有哪些带地暖的户型”）。
  - **展示结果**：下方网格卡片展示命中的文档片段，高亮关键词，并显示匹配度百分比（如“匹配度 87%”）。若无结果，显示“暂无相关片段，建议补充上传资料”。

---

### 模块 3.4：Skill 市场（能力插件中心）

允许管理员无需重启系统，随时安装或卸载新功能（如风水分析、风水避讳检测等）。

- **3.4.1 技能商店概览**
  - **视图**：卡片瀑布流布局。
  - 每张卡片显示：Skill图标、名称、一句话简介、版本号、是否开源标识（绿色🟢开源/灰色🔒专有）。
  - **状态标识**：【未安装】灰色按钮 / 【已安装】蓝色“已启用”标签。

- **3.4.2 安装与卸载交互**
  - **安装**：点击【安装】，按钮变为“安装中...（带旋转圈）”，成功后Toast提示“安装成功”，卡片状态自动刷新为“已启用”。
  - **卸载**：已安装的卡片右下角显示【卸载】文字链接。点击后弹出警示框：“卸载后，对话中将无法使用该能力，确认卸载吗？”
  - **特别注意**：若该Skill正在被后台异步任务调用（如正在渲染图片），卸载按钮置灰，鼠标悬浮提示“该技能正在执行任务，请稍后重试”。

---

### 模块 3.5：任务中心（长时操作处理）

专门处理那些“需要等几十秒甚至几分钟”的事情，让用户不用傻傻盯着屏幕。

- **3.5.1 入口与列表**
  - **入口**：顶部导航栏右侧的【⏳ 钟表/铃铛】图标，角标显示“进行中任务数量”（如“2”）。
  - **列表字段**：任务名称、提交时间、**进度条（0%~100%）**、状态（排队中/执行中/已完成/失败/已取消）。

- **3.5.2 针对任务的操作按钮**
  - **进行中**：显示【取消任务】按钮（红色文字）。点击后任务状态变为“已取消”，Worker停止计算。
  - **已完成**：显示【查看结果】按钮。点击弹出模态框展示结果（效果图直接展示预览图，提供【下载高清图】按钮；批量导入显示“成功XX条，失败XX条”，提供【下载错误报告】）。
  - **失败**：显示【🔄 重新尝试】按钮。点击后复制原参数提交新任务，原失败记录保留作为历史参照。

---

### 模块 3.6：系统监控与成本看板（仅管理员可见）

- **3.6.1 对话审计日志**
  - **入口**：顶部导航栏用户名下拉 -> 【审计日志】。
  - **页面**：表格展示全部门用户的对话摘要。包含：时间、用户、调用的Skill、消耗预估费用（元）。
  - **操作**：点击任一行右侧的【查看完整对话】按钮，侧滑抽屉展示该次完整的人机对话原文。

- **3.6.2 费用统计看板**
  - **可视化**：柱状图展示“近7天/本月API调用费用趋势”。
  - **预警条**：当费用超过预设阈值时，页面顶部出现红色横幅：“⚠️ 本月费用已达预算的 80%”。
  - **排行榜**：下方列表展示“费用消耗Top 3的Skill”（便于决定是否优化调用频率）。

---

### 模块 3.7：系统设置与多租户管理（仅管理员）

- **3.7.1 用户管理**
  - **页面**：表格展示所有成员（姓名、账号、角色、所属部门）。
  - **操作**：【添加用户】按钮（填写账号/初始密码/分配角色）。支持批量导入成员列表（CSV）。
  - **登录方式切换**：开关选项“启用LDAP域账号登录”（开启后登录页显示“域账号登录”Tab）。

- **3.7.2 数据隔离感知**
  - 管理员在添加用户时，必须选择“所属工作空间（Workspace/部门）”。选定后，该用户登录后**永远看不到其他部门上传的知识库文档和对话记录**。

---

## 四、端到端典型业务操作流程（用户故事）

### 场景一：策划做一份户型营销方案
1. **打开浏览器**登录系统 -> 进入【智能对话】。
2. 点击输入框下方的【📎 回形针】，上传“楼盘A的户型图.jpg”。
3. 在输入框输入：“`@户型解析` 帮我分析这个户型的优缺点，并写一段卖点文案”。
4. 点击发送，顶部状态条闪烁，等待约10~15秒。
5. 查看AI回复：包含户型结构化参数和300字朋友圈文案。
6. *（若发现AI把“主卧朝南”识别成了“朝北”）* -> 点击回复卡片下的【📐 打开户型编辑】。
7. 在右侧表单中将“朝向”下拉改为“南” -> 点击【确认并保存】。
8. 回到对话窗口，复制修改后的最终版文案，完成工作。

### 场景二：管理员上线新政策文件
1. 点击顶部导航【知识库管理】。
2. 点击【📤 上传文档】，拖入“2025年房贷新政.pdf”。
3. 因系统已有旧版同名文件，弹窗选择：“作为新版本上传（旧版本自动失效）”。
4. 等待进度条100%变为“生效中”状态。
5. 在顶部【🔍 试搜】输入“房贷利率”，验证新文档内容是否被检索命中，确认无误后退出。

---

## 五、附录：异常与边界状态体验规范

为了保证B端软件的严谨性，以下极端情况必须有明确的UI展示：

| 场景 | 前端界面表现 | 用户可操作项 |
| :--- | :--- | :--- |
| **AI拒答（知识不足）** | 回复区显示黄色信息条：“当前知识库暂无相关内容为您解答。建议您上传相关楼盘资料。” | 【📤 去上传资料】一键跳转按钮 |
| **网络断开/弱网超时** | 页面顶部固定显示红色Banner：“网络不稳定，AI服务已中断，请检查网络后重试。” | 【🔄 重连】按钮 |
| **户型解析彻底失败** | 编辑器页面显示大号红色异常态图形：“无法识别该图纸，请确认文件是否清晰或为DXF格式。” | 【重新上传】按钮 |
| **效果图渲染超时（超过5分钟）** | 任务中心该任务卡片状态变为“生成超时”，但保留半成品缩略图（若有）。 | 【重新生成】按钮 / 【删除任务】按钮 |
| **系统内部错误（500）** | 全局弹窗提示：“系统繁忙，请稍后重试。错误追踪码：TP-XXXX-XXXX” | 【📋 一键复制追踪码】供报修使用 |

---

**文档结束**

此文档可直接用于：
1. **UI设计稿绘制**（按模块画原型）。
2. **业务方培训**（明确他们能干什么）。
3. **验收测试用例编写**（检查每个按钮点击后是否符合描述）。

如有任何页面功能逻辑需要进一步细化（例如“楼盘下拉框是否支持模糊搜索”等），请告知，我即刻补充。
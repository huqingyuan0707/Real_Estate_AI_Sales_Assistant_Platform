# 等保三级合规自查清单（房地产 AI 销售助手平台）

> 自动生成接口：`GET /api/v1/compliance/report`（需 audit 权限），本清单与其对应，
> 每次自查可拉取最新运行时证据。生成日期：2026-09-11。

## 一、控制点对照（代码层已实现）

| 控制点 | 要求 | 实现证据 | 状态 |
|---|---|---|---|
| 身份鉴别-双因素 | 两种或以上鉴别技术 | TOTP 动态口令（RFC 6238 零依赖实现）+ 静态密码；`/auth/totp/setup·enable·disable` | ✅ |
| 身份鉴别-失败处理 | 登录失败处理与锁定 | 连续失败 5 次锁定 15 分钟（`services/password_policy.py`，实测 429/2002） | ✅ |
| 身份鉴别-口令策略 | 口令复杂度 | 最小 8 位 + 大小写 + 数字，创建用户时强制校验 | ✅ |
| 身份鉴别-弱口令 | 无默认/弱口令 | 演示账号仍为 `123456`，**生产上线前必须重置**（`admin/wangmin/liqiang/zhangwei`） | ⚠️ |
| 访问控制-RBAC | 最小权限 | 角色-权限矩阵 + `require_perm` 接口级授权（admin/manager/member） | ✅ |
| 访问控制-数据级 | 越权防护 | 知识密级（公开/内部/机密，机密仅 admin）+ 租户/部门过滤，检索与生成双阶段 | ✅ |
| 安全审计-覆盖 | 每用户每事件 | RAG 问答审计（提问/命中文档/耗时/成本/治理拦截）+ 登录审计 + 登出 | ✅ |
| 安全审计-防篡改 | 审计记录保护 | **审计哈希链**（SHA256 链式，`/audit/verify` 可重算校验定位断点） | ✅ |
| 安全审计-追溯 | 全链路 Trace | trace_id 贯穿 + Langfuse 适配器（HTTP ingestion，未配置仅本地 JSONL） | ✅ |
| 入侵防范 | 防恶意输入 | RAG 提示注入 8 类特征扫描 + system 最高优先级约束 + 治理拦截告警 | ✅ |
| 数据完整性-传输 | 传输加密 | 应用层就绪；**由部署层落实 Nginx/网关 HTTPS（TLS 1.2+）** | 🔧 |
| 数据保密性-展示 | 鉴别信息保密 | 密码哈希存储；用户 AI Key 只回掩码；输出 PII 脱敏（手机/身份证/银行卡/邮箱） | ✅ |
| 数据保密性-分级 | 分类分级与脱敏 | 知识密级标注；长期记忆敏感信息强制过期 | ✅ |
| 备份恢复 | 重要数据备份 | 索引版本快照（含向量）+ 一键回滚 + SHA256 增量同步 | ✅ |
| 剩余信息保护 | 鉴别信息及时清除 | Token 有效期 12 小时自动失效；登出清除本地存储 | ✅ |

## 二、部署层待办（代码无法覆盖，上线前人工确认）

- [ ] 前置 Nginx/网关启用 HTTPS（TLS 1.2+），禁用弱密码套件
- [ ] 重置全部演示账号口令（`admin/123456` 等），并启用 TOTP
- [ ] `SECRET_KEY` 从默认值改为强随机（环境变量注入）
- [ ] 数据库/向量库迁移至独立实例并启用传输与存储加密（Milvus + TLS，`VECTOR_BACKEND=milvus`）
- [ ] 审计日志外发到独立日志服务器（哈希链随行），保留期 ≥ 6 个月
- [ ] Langfuse 自托管实例接入（`LANGFUSE_*` 配置），Trace 保留期与等保要求对齐
- [ ] 限流阈值按生产容量调整（`RAG_RATE_LIMIT_PER_MIN`），多副本时切换集中式存储
- [ ] OIDC 接入真实 IdP（企业微信/Keycloak/Azure AD），关闭 `OIDC_MOCK` 并启用 JWKS 签名验证

## 三、自查命令

```powershell
cd backend
.\.venv\Scripts\python.exe tests\smoke_enterprise.py   # 企业级能力 20 项冒烟（含双因素/OIDC/审计链）
# 合规报表：GET /api/v1/compliance/report（admin/audit 权限）
# 审计链校验：GET /api/v1/audit/verify
```

## 四、外部存储与解析扩展说明（本清单相关能力）

- 向量后端：`VECTOR_BACKEND=chroma|milvus`（Milvus 不可用自动回退并记录原因，`/compliance/report.runtime` 可见）
- 关键词后端：`KEYWORD_BACKEND=builtin|elasticsearch`（回退内置 BM25）
- 解析扩展：PPT（python-pptx）、HTML（标准库）、图片与扫描件 PDF（OCR：pytesseract + Tesseract + PyMuPDF，缺失时返回带安装指引的错误）

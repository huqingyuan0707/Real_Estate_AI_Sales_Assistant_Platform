# 文档一：《UI设计稿与交互原型规格书》

| 文档属性 | 内容 |
| :--- | :--- |
| 文档版本 | V1.0 |
| 适用阶段 | UI设计、原型制作、交互评审 |
| 交付物形式 | 本规格书 + Figma/Sketch源文件（按本规格绘制） |
| 设计工具建议 | Figma（推荐，便于协作和原型演示） |


## 第一章：设计前提与画板设置

### 1.1 画板尺寸
- **桌面端基准**：1440 x 900 px（设计稿以此为准，实际开发适配1280px及以上）
- **响应式断点**：仅做桌面端（≥1024px），暂不涉及移动端/平板独立设计

### 1.2 栅格系统
- **12列栅格**，列宽自适应，间距（Gutter）24px
- **左右安全边距**：24px（内容区与浏览器边缘间距）
- **布局宽度**：最大内容区 1392px（1440-48）

### 1.3 文件组织（Figma页面分层建议）
| 页面名称 | 包含内容 |
| :--- | :--- |
| 1. Design System | 颜色、字体、图标、组件库 |
| 2. 全局框架 | 左侧导航 + 顶部状态栏（Master Component） |
| 3. 智能对话 | 对话页全状态（空态/流式输出/历史列表） |
| 4. 户型编辑器 | 左右分屏 + 校验状态 |
| 5. 知识库管理 | 列表页 + 上传弹窗 + 版本冲突弹窗 |
| 6. Skill市场 | 卡片浏览 + 安装状态 |
| 7. 任务中心 | 任务列表 + 结果弹窗 |
| 8. 系统设置 | 审计日志 + 费用看板 + 用户管理 |
| 9. 异常态集合 | 所有空状态/错误状态/加载状态 |


## 第二章：全局框架（Master Frame）

### 2.1 左侧导航栏（宽度：240px，背景：#0F172A）

**顶部Logo区（高：56px）**
- 左内边距16px，居中对齐
- Logo：图标（24x24px）+ 产品名“AI销售助手”（字号16px，加粗，白色）

**菜单列表（flex列，间距4px）**
| 菜单项 | 图标 | 文字 | 状态样式 |
| :--- | :--- | :--- | :--- |
| 智能对话 | 💬 | 智能对话 | 激活态：背景#1E293B，左侧3px #2B5CF5蓝条 |
| 户型工具 | 📐 | 户型工具 | 悬停态：背景#1E293B |
| 知识库管理 | 📚 | 知识库管理 | — |
| Skill市场 | 🧩 | Skill市场 | — |
| 任务中心 | ⏳ | 任务中心 | 带角标（红色圆点，显示任务数） |
| — | — | — | 分割线（#334155，高1px，上下间距8px） |
| 系统设置 | ⚙️ | 系统设置 | 仅管理员可见 |

**底部用户区（高：64px）**
- 用户头像（圆形，32px，灰色占位）+ 用户名（14px，白色）+ 角色标签（“管理员”/“成员”，12px，#94A3B8）
- 下拉箭头，点击弹出退出登录选项

### 2.2 顶部状态栏（高度：64px，背景：#FFFFFF，底部1px #E2E8F0）

| 位置 | 内容 | 规格 |
| :--- | :--- | :--- |
| 左侧 | 当前页面标题（如“智能对话”） | 字号20px，加粗，#0F172A |
| 右侧（flex对齐） | 全局搜索框（宽度240px） | 圆角8px，高度36px，带搜索图标，占位文字“搜索...” |
| 右侧 | 任务中心铃铛图标 | 24x24px，右上角带红色角标（数字） |
| 右侧 | 用户头像 | 32px圆形，点击下拉菜单（个人设置/退出登录） |


## 第三章：智能对话工作台（完整页面原型）

### 3.1 页面布局（左侧列表 280px / 右侧对话区 剩余宽度）

#### 3.1.1 左侧会话列表（背景：#F8FAFC）
- **顶部**：【✨ 新建对话】按钮（宽度100%，高度40px，主色填充，圆角8px）
- **列表分组**：
  - 标题“今天”（12px，#94A3B8，上下间距8px）
  - 会话条目：圆角8px，高度44px，左右内边距12px
    - 内容：对话首句（截断12字）+ 右侧时间（12px，#94A3B8）
    - 悬停：背景变#F1F5F9，右侧出现铅笔+垃圾桶图标
    - 激活态：背景#EFF6FF，左侧3px主色竖条

#### 3.1.2 对话消息流区域（背景：#FFFFFF，内边距24px）
- **消息气泡规范**：
  - **用户消息**：靠右，背景#2B5CF5，白色文字，#1E293B，字号15px，行高1.6，圆角16px（右下小圆角4px），最大宽度70%，内边距12px 16px
  - **AI消息**：靠左，背景#F8FAFC，文字#1E293B，字号15px，行高1.6，圆角16px（左下小圆角4px），最大宽度80%，内边距12px 16px
  - **AI生成标签**：气泡左上角，灰底白字“AI生成 仅供参考”，字号10px，圆角4px，内边距2px 8px
- **流式状态条**（AI气泡上方）：
  - 灰色胶囊背景（#F1F5F9），高度28px，圆角20px，内边距8px 16px
  - 内容：三个跳动点动画 + 文字（“🧠 规划中...”→“⚙️ 执行中...”→“✅ 校验中...”→“✍️ 生成回答”）
  - 完成后自动消失
- **引用来源折叠**：
  - AI气泡底部虚线分割线（#E2E8F0）
  - 蓝色文字链接“📎 引用 N 篇资料”（14px，#2B5CF5）
  - 点击展开：下拉列表展示文档名 + 页码（灰色小字）

#### 3.1.3 底部输入区（高度：140px，背景：#FFFFFF，顶部1px #E2E8F0）
- **输入框**：多行文本域（flex:1），最小高度56px，最大100px，边框无，占位文字“输入需求，或 @ 调用技能，支持上传附件...”，字号14px
- **附件标签栏**（输入框上方）：以标签形式展示已选文件（“户型图.jpg ✕”），浅灰背景，圆角4px
- **工具栏**（输入框下方，高度40px）：
  - 左侧：【📎 附件上传】图标按钮（24x24px）
  - 右侧：【发送】按钮（主色填充，宽80px，高36px，圆角8px，输入为空时置灰）


## 第四章：户型设计编辑器（工具页）

### 4.1 页面布局（左60% / 右40%，无间隙）

#### 4.1.1 左侧图像预览区（背景：#F1F5F9）
- **顶部工具栏**（高度44px，白色背景）：
  - 左侧：缩放百分比显示（“100%”）
  - 右侧：【放大镜+】、【缩小镜-】、【1:1重置】图标按钮
- **中央画布**：展示户型图片/DXF，支持拖拽平移，居中显示
- **加载失败态**：中央大号异常图标（64px）+ 标题“图纸解析失败”+ 副标题“请确认文件是否清晰或为DXF格式”+ 【重新上传】按钮
- **底部信息栏**（高度32px，灰色背景）：文件名 + 分辨率

#### 4.1.2 右侧参数编辑区（背景：#FFFFFF，内边距24px）
- **顶部**：标题“结构化参数（可编辑）”（14px，#64748B）+ 状态指示灯（绿色圆点“校验通过”/红色圆点“存在 N 处错误”）
- **表单分组**（间距16px）：
  - **基础信息**：户型名称（输入框，宽度100%）、所属楼盘（下拉搜索框，宽度100%）
  - **面积与尺寸**：建筑面积（数字输入框，宽度100%）、套内面积（数字输入框，宽度100%）
  - **格局**：几室几厅（数字加减器）、朝向（下拉选择）
- **校验反馈**：
  - 错误状态：输入框边框变红（#EF4444），底部红色小字提示（12px）
  - 正确状态：输入框右侧绿色对勾图标（16x16px）
- **底部操作栏**（高度72px，白色背景，顶部1px #E2E8F0，固定底部）：
  - 左侧：状态文字（“✅ 校验通过，可提交” / “❌ 存在 2 处错误”）
  - 右侧：【强制忽略并提交】（灰色文字链接）+ 【确认并保存】（主色按钮，校验不通过时置灰）


## 第五章：知识库管理（列表页）

### 5.1 顶部操作栏（高度64px，白色背景，底部1px #E2E8F0）
- 左侧：【📤 上传文档】主色按钮（高度40px）
- 右侧：【🔍 试搜一下】搜索框（宽度300px，高度40px），带搜索图标

### 5.2 数据表格（全宽，内边距24px）
- **表头**：文档名称 / 类型 / 大小 / 状态 / 版本 / 更新时间 / 操作（背景#F8FAFC，高度44px，字号12px，#64748B）
- **表格行**：高度52px，字号14px，悬停背景#F8FAFC
- **状态标签样式**：
  - 🟢 生效中：背景#D1FAE5，文字#065F46，圆角12px，内边距2px 10px
  - 🟡 处理中：背景#FEF3C7，文字#92400E，带旋转圈
  - ⚪ 已失效：背景#F1F5F9，文字#64748B
- **操作列**：【查看】（蓝色文字链接）| 【停用】（灰色文字，弹窗二次确认）

### 5.3 上传弹窗（尺寸：640 x 480px，居中模态框）
- **拖拽区**：虚线方框（#E2E8F0，虚线2px），中央云朵图标（48px）+ “拖拽文件至此，或点击选择文件”（16px，#1E293B）+ 小字“支持 PDF、Word、Excel、JPG、DXF（≤50MB）”（12px，#94A3B8）
- **文件列表**（拖拽后显示）：文件名 + 大小 + 状态文字（“等待上传”/“解析中”/“✅ 完成”）
- **底部按钮**：【取消】| 【确认上传】（主色）

### 5.4 版本冲突弹窗（尺寸：480px，优先级高于上传弹窗）
- 标题：“⚠️ 检测到同名文档”（16px，加粗）
- 正文：“已存在文档“2025政策汇编.pdf”，请选择处理方式：”
- 单选按钮：
  - ○ 覆盖旧版本（旧文档归档，不可恢复）
  - ○ 作为独立新文档保留（两个版本并存）
- 底部按钮：【取消】| 【确认上传】（主色）


## 第六章：Skill市场（卡片页）

### 6.1 页面头部（高度80px）
- 标题：“Skill 市场”（20px，加粗）+ 描述“安装即用，扩展AI能力”（14px，#64748B）
- 右侧筛选：【全部 / 已安装 / 未安装】按钮组（三段式切换）

### 6.2 卡片网格（4列，间距16px）
- **卡片尺寸**：宽度100%，最小高度200px，白色背景，圆角12px，阴影（0 2px 8px rgba(0,0,0,0.06)）
- **卡片内容**（内边距16px，flex列，space-between）：
  - 左上角：开源标识（绿色“开源”标签 / 灰色“专有”标签，圆角4px，字号10px）
  - 中央：图标（48x48px，圆形）+ 标题（16px，加粗）+ 描述（14px，#64748B，截断2行）+ 版本号（12px，#94A3B8）
  - 底部操作区：
    - 未安装：【安装】主色细边框按钮（高度32px，圆角6px，悬停变实心）
    - 已安装：✅ 已启用（绿色标签）+ 灰色【卸载】文字链接
- **安装进度**：按钮文字变为“安装中...”+ 旋转圈，禁用点击


## 第七章：任务中心（进度监控页）

### 7.1 统计卡片（顶部3列，间距16px）
- 卡片尺寸：白色背景，圆角12px，内边距16px
- 内容：数值（24px，加粗）+ 标签（14px，#64748B）
- 三类：进行中（X个）/ 已完成（X个）/ 失败（X个）

### 7.2 任务列表（卡片式，每行一个）
- **任务行**：白色背景，圆角12px，内边距16px，间距12px，flex居中
- **内容布局**：
  1. 状态图标（24x24px：执行中旋转圈/完成绿色对勾/失败红色感叹号）
  2. 任务名称（16px，#1E293B，flex:1）
  3. 提交时间（14px，#94A3B8）
  4. 进度条（宽度200px，高度6px，灰色背景，主色填充，带百分比文字）
  5. 状态文字（14px，对应颜色）
  6. 操作按钮：
     - 执行中：【取消任务】（红色文字，14px）
     - 已完成：【查看结果】（主色文字，14px）
     - 失败：【🔄 重新尝试】（主色文字，14px）

### 7.3 查看结果弹窗（尺寸：800px，居中）
- 标题：“任务结果”（16px，加粗）
- 内容区：
  - 效果图：大图预览（宽度100%，最大高度400px，object-fit:contain）+ 【下载高清图】主色按钮
  - 批量导入：“✅ 成功导入 150 条，❌ 失败 3 条”+ 【下载失败明细.csv】按钮
- 底部：【关闭】按钮


## 第八章：系统设置（管理员专用）

### 8.1 审计日志页
- **筛选栏**：日期范围选择器 + 用户下拉选择 + 【查询】按钮（高度40px）
- **表格**（同知识库表格样式）：操作时间 / 用户 / 操作类型 / 关联Skill / 费用消耗(元) / 【查看详情】
- **详情抽屉**（从右侧滑出，宽度480px）：展示该次完整对话原文，灰色背景，等宽字体

### 8.2 费用看板
- **折线图**（宽度70%）：X轴日期，Y轴费用，趋势线主色
- **预警横幅**（图表上方）：红色背景（#FEE2E2），文字“⚠️ 本月费用已达预算的 80%”，内边距12px
- **消耗榜单**（右侧30%）：Top 3 Skill名称 + 柱状图（灰色背景，主色填充比例）

### 8.3 用户管理页
- **顶部**：【添加用户】主色按钮
- **表格**：姓名 / 账号 / 角色（标签“管理员”#EFF6FF蓝 / “成员”#F1F5F9灰） / 所属部门 / 状态（启用/禁用） / 【编辑】【重置密码】【停用】


## 第九章：异常态与空状态集合（独立画板，供全局复用）

| 状态名称 | 应用场景 | 视觉规范 |
| :--- | :--- | :--- |
| 空状态（通用） | 对话历史为空 / 任务列表为空 / Skill无结果 | 中央插画（64px图标）+ 标题“暂无内容”（16px，#1E293B）+ 副标题引导（14px，#94A3B8） |
| AI拒答 | 对话区AI消息 | AI气泡内黄色背景条（#FEF3C7），左侧⚠️图标，文字“知识库暂无相关内容”，下方【📤 去上传资料】主色文字按钮 |
| 网络断连 | 全局顶部 | 红色粘性横幅（#FEE2E2），高度40px，文字“网络已断开，请检查连接”，右侧【重新连接】按钮 |
| 加载骨架屏 | 知识库列表 / 任务列表 | 灰色渐变动画块（Shimmer），宽度100%，高度与真实列表相同 |
| 权限不足 | 点击无权限菜单 | 遮罩层弹窗：“您无权访问此模块，请联系管理员”，唯一按钮【我知道了】 |

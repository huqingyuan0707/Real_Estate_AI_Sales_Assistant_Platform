## 一句话定义

**企业级 RAG 知识智能问答** = 以企业知识为底座，通过 **数据治理、解析分块、嵌入检索、重排生成、权限过滤、引用溯源、评估迭代、安全合规、可观测运维**，让 Agent 在权限范围内给出**准确、可追溯、可更新、可运营**的答案，而不是一个“能聊天但不可信”的知识玩具。

---

## 总体架构简图

```text
数据源：文档 / Wiki / 工单 / 数据库 / 网页 / 邮件 / IM / 代码
        ↓
数据治理：采集 → 清洗 → 分类分级 → 权限标注 → 版本管理
        ↓
知识加工：解析 → 分块 → 嵌入 → 索引 → 元数据 → 质量校验
        ↓
存储：向量库 | 全文索引 | 关系库 | 对象存储 | 图数据库
        ↓
检索：Query 理解 → 多路召回 → 融合 → 重排 → 权限过滤
        ↓
生成：Prompt 组装 → 模型生成 → 引用绑定 → 幻觉检测
        ↓
服务：问答 API | 流式输出 | 反馈 | 评估 | 审计 | 监控
```

---

## 一、数据治理：先治理，再 RAG

**核心原则：垃圾进，垃圾出。RAG 效果 70% 取决于数据质量。**

- **数据源盘点**：文档、Wiki、工单、FAQ、邮件、IM、数据库、代码、网页。
- **采集方式**：API、爬虫、CDC、文件同步、手工上传。
- **清洗**：去重、去噪、格式统一、乱码修复、OCR 纠错。
- **分类分级**：公开 / 内部 / 机密 / 绝密，决定可见范围。
- **权限标注**：每条知识必须带 `tenant_id`、`dept_id`、`role`、`acl`。
- **版本管理**：知识有版本、生效时间、失效时间。
- **更新机制**：增量更新、定时同步、变更触发。
- **质量校验**：完整性、准确性、时效性、一致性。
- **生命周期**：创建 → 审核 → 发布 → 更新 → 归档 → 删除。

**企业级必备：**
- 知识有 Owner，有人负责。
- 知识有权限，不能越权检索。
- 知识有版本，可回溯。
- 知识有审核，不能随便进库。

---

## 二、文档解析与分块

**解析：**
- 格式：PDF、Word、Excel、PPT、HTML、Markdown、图片、扫描件。
- 工具：PyMuPDF、pdfplumber、Unstructured、OCR（PaddleOCR / Tesseract）。
- 难点：表格、公式、多栏、页眉页脚、扫描件、图文混排。
- 保留结构：标题层级、列表、表格、代码块、页码。

**分块策略：**
- 固定长度：简单，但易切断语义。
- 语义分块：按段落、标题、章节。
- 递归分块：按层级递归切分。
- 父子分块：小块检索，大块生成。
- 滑动窗口：重叠保留上下文。
- 表格/代码单独处理。

**元数据：**
- 来源、标题、章节、页码、作者、时间、版本。
- 权限标签、租户、部门、密级。
- 业务标签：产品、地区、客户、流程。

**企业级必备：**
- 分块保留结构与来源。
- 元数据完整，支持过滤。
- 表格、代码、图片单独处理。
- 分块大小可配置、可评估。

---

## 三、嵌入与索引

**嵌入模型：**
- 通用：OpenAI、BGE、M3E、GTE、E5。
- 中文优化：BGE、M3E、Conan。
- 领域微调：用企业语料微调。
- 多语言：按需选择。

**索引：**
- 向量库：pgvector、Qdrant、Milvus、Weaviate、Chroma。
- 全文索引：Elasticsearch、OpenSearch、PostgreSQL FTS。
- 混合索引：向量 + 关键词 + 结构化过滤。
- 元数据过滤：租户、权限、时间、业务标签。

**要点：**
- 嵌入模型版本管理，换模型要重建索引。
- 索引分片、副本、性能调优。
- 增量索引，避免全量重建。
- 索引与源数据一致性校验。

**企业级必备：**
- 嵌入模型可替换、可评估。
- 索引支持权限过滤。
- 支持增量更新。
- 索引版本可回滚。

---

## 四、检索：多路召回 + 重排

**Query 理解：**
- 改写：口语转书面、补全上下文。
- 扩展：同义词、缩写、别名。
- 分解：复杂问题拆成子问题。
- 意图识别：问答、查询、操作、闲聊。
- 多轮改写：结合对话历史。

**召回：**
- 向量召回：语义相似。
- 关键词召回：BM25、全文检索。
- 混合召回：向量 + 关键词。
- 多路召回：不同模型、不同索引。
- 结构化查询：数据库、API。

**融合与重排：**
- RRF、加权融合。
- 重排模型：BGE-Reranker、Cohere Rerank。
- LLM 重排：用大模型打分。
- 去重、去噪、多样性。

**权限过滤：**
- 检索前过滤：只召回用户可见知识。
- 检索后过滤：召回后再校验权限。
- 多租户隔离：`tenant_id` 强制过滤。
- 密级控制：按用户密级过滤。

**企业级必备：**
- 多路召回 + 重排。
- 权限过滤在检索阶段完成。
- 引用来源可追溯。
- 检索结果可解释。

---

## 五、生成：准确、引用、可控

**Prompt 组装：**
- 系统提示：角色、边界、格式。
- 上下文：检索结果 + 引用编号。
- 用户问题：原始 + 改写。
- 约束：只基于上下文回答，不知道就说不知道。
- 输出格式：结构化、引用标注。

**生成控制：**
- 温度、Top-P、最大 Token。
- 禁止编造，无依据不回答。
- 引用绑定：每句话对应来源。
- 幻觉检测：与上下文一致性校验。
- 敏感信息过滤。

**回答结构：**
- 直接答案。
- 依据引用。
- 相关推荐。
- 不确定时明确说明。

**企业级必备：**
- 引用可点击、可追溯。
- 无依据不回答。
- 敏感信息不泄露。
- 输出格式统一。

---

## 六、权限与多租户

**核心原则：用户只能看到自己有权限看到的知识。**

- 认证：SSO / OIDC / JWT。
- 授权：RBAC / ABAC。
- 多租户：`tenant_id` 贯穿全链路。
- 数据隔离：命名空间、数据库、行级。
- 知识权限：文档级、段落级、字段级。
- 检索过滤：向量库元数据过滤 + 应用层校验。
- 生成过滤：输出前再校验。
- 审计：谁、何时、问了什么、看了哪些知识。

**企业级必备：**
- 权限过滤在检索和生成两阶段都做。
- 多租户强隔离。
- 越权检测与告警。
- 全链路审计。

---

## 七、评估：没有评估就没有优化

**离线评估：**
- 黄金数据集：问题 + 标准答案 + 标准引用。
- 指标：召回率、准确率、MRR、NDCG、答案正确率、引用准确率、幻觉率。
- LLM-as-judge：自动打分。
- 回归测试：每次变更跑一遍。

**在线评估：**
- 用户反馈：点赞、点踩、纠错。
- 采纳率、解决率、转人工率。
- A/B 测试：不同模型、分块、检索策略。
- 影子流量：新版本对比。

**发布门禁：**
- 评测不达标不发布。
- 关键指标下降自动回滚。

**企业级必备：**
- 有黄金数据集。
- 有自动化评估。
- 有发布门禁。
- 有反馈闭环。

---

## 八、安全与合规

- 提示注入防护：检索内容可能含恶意指令。
- 越权检测：防止用户套取无权限知识。
- PII 脱敏：输出前过滤。
- 数据加密：传输、存储。
- 合规：等保、GDPR、SOC2、数据本地化。
- 审计：全链路日志，不可篡改。
- 模型安全：输入输出过滤。
- 知识安全：密级、水印、溯源。

**企业级必备：**
- 提示注入防护。
- 越权检测。
- PII 脱敏。
- 全链路审计。
- 合规认证。

---

## 九、可观测与运营

**指标：**
- 检索：召回率、延迟、命中率。
- 生成：Token、延迟、成本、幻觉率。
- 业务：问题解决率、采纳率、转人工率。
- 系统：QPS、错误率、可用性。

**日志与追踪：**
- 每次问答：Query、召回、重排、Prompt、生成、引用。
- Trace ID 贯穿全链路。
- Langfuse / LangSmith 做 LLM Trace。

**运营：**
- 知识热度、缺口分析。
- 低质量知识识别。
- 用户反馈驱动知识更新。
- 定期评估与调优。

**企业级必备：**
- 全链路 Trace。
- 质量、成本、安全三类指标。
- 知识运营闭环。
- 持续迭代。

---

## 十、成本与性能

**成本：**
- 嵌入成本、检索成本、生成成本。
- 缓存：Query 缓存、检索缓存、生成缓存。
- 小模型路由：简单问题用小模型。
- 批处理、异步。
- 成本归因：按租户 / 部门 / 场景。

**性能：**
- 检索延迟、生成延迟、端到端延迟。
- 索引优化、并行召回、流式输出。
- 预热、缓存、CDN。
- 限流、降级。

**企业级必备：**
- 成本可统计、可归因。
- 延迟可接受。
- 缓存与降级策略。

---

## 十一、常见坑

| 坑 | 后果 | 对策 |
|---|---|---|
| 数据没治理 | 垃圾进垃圾出 | 先治理再 RAG |
| 分块不合理 | 语义切断 | 语义分块 + 父子块 |
| 只做向量召回 | 漏召回 | 混合召回 + 重排 |
| 不做权限过滤 | 越权泄露 | 检索 + 生成双过滤 |
| 不做引用 | 不可信 | 引用绑定 + 可追溯 |
| 不做评估 | 无法优化 | 黄金集 + 自动评估 |
| 不做幻觉检测 | 编造答案 | 一致性校验 + 无依据不答 |
| 不做更新 | 知识过期 | 增量更新 + 生命周期 |
| 不做审计 | 无法追责 | 全链路审计 |
| 不做成本控制 | 成本失控 | 配额 + 缓存 + 小模型 |

---

## 十二、优先级路线图

**P0：立即做**
- 数据治理：采集、清洗、权限标注。
- 解析分块：结构化、元数据。
- 嵌入索引：向量 + 全文。
- 检索：混合召回 + 重排。
- 生成：Prompt + 引用。
- 权限过滤：检索 + 生成。
- 评估：黄金集 + 基础指标。

**P1：2-4 周**
- 多路召回、Query 改写。
- 幻觉检测、PII 脱敏。
- 多租户、审计。
- 可观测：Trace、指标。
- 反馈闭环。
- 增量更新。

**P2：持续**
- 领域微调、重排优化。
- A/B、影子流量。
- 知识运营、缺口分析。
- 成本优化、性能调优。
- 合规认证。

---

## 检查清单

- 数据是否治理：清洗、分类、权限、版本？
- 分块是否保留结构与元数据？
- 嵌入模型是否可替换、可评估？
- 是否混合召回 + 重排？
- 是否做 Query 改写与多轮理解？
- 权限过滤是否在检索和生成两阶段都做？
- 是否多租户强隔离？
- 回答是否带引用、可追溯？
- 是否无依据不回答、防幻觉？
- 是否防提示注入、越权、PII 泄露？
- 是否有黄金数据集和自动评估？
- 是否有发布门禁和反馈闭环？
- 是否全链路 Trace 与审计？
- 是否成本可统计、可归因？
- 是否增量更新与生命周期管理？
- 是否有知识运营和缺口分析？

---

## 一句话总结

**企业级 RAG 知识智能问答 = 数据治理 + 解析分块 + 混合检索 + 重排生成 + 权限过滤 + 引用溯源 + 评估迭代 + 安全合规 + 可观测运营，核心不是“能答”，而是“答得准、答得全、答得安全、答得可追溯、答得可运营”。**
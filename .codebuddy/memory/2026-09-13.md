# 2026-09-13

## 用户再次遇到 push 报错：Repository not found（复现既有结论，未解决待用户建仓）
- 现象：在 `frontend` 目录执行 `git push -u origin main` → `ERROR: Repository not found.`
- 复核结果（与 2026-09-12 结论一致）：
  - 仓库根是 `D:/Real_Estate_AI_Assistant_Platform`（frontend 是子目录，无嵌套 .git）
  - origin = `git@github.com:huqingyuan0707/real-estate-ai-sales-assistant-platform.git`
  - `ssh -T git@github.com` → `Hi huqingyuan0707!` 认证正常（用的是 `~/.ssh/id_ed25519`）
  - `git ls-remote origin` → Repository not found；GitHub API `repos/...` → **404**
  - 无 proxy/url rewrite 干扰（`HTTP_PROXY` 等为空，ssh config 无 github 段）
  - 本地 `main = c9f389d`（完善项目ai对话）← `7f31a1b`（初始化仓库/改 .gitignore），工作区干净；`origin/main = 860a131` 是**残留 ref**（ahead 2 / behind 1 由此而来）
  - 未装 `gh` CLI
- 结论：**远程仓库在 GitHub 上已不存在（被删或改名）**，SSH 密钥与账号权限都没问题，纯粹是远端无仓

## 已解决 URL 问题：仓库是【改名】了，不是删除
- 正确地址（用户提供）：`git@github.com:huqingyuan0707/Real_Estate_AI_Sales_Assistant_Platform.git`
  - 注意差异：是**下划线 + 全大写** `Real_Estate_AI_Sales_Assistant_Platform`，旧配置是连字符小写 `real-estate-ai-sales-assistant-platform` —— 这才是 404 的原因
- 已执行 `git remote set-url origin <新地址>`；`git ls-remote` 通过，远程 main = `860a131`（与本地残留 origin/main 一致，佐证是同一仓库改名）
- **本地与远程历史无共同祖先**（unrelated histories），`git push` 会被拒：
  - `main` = 2 提交，`7f31a1b` 是**全新根提交**（无 parent）→ `c9f389d`（= 9-12 重写历史那次，219 文件干净版）
  - `origin/main` = 1 提交 `860a131`（旧的 3.4 万文件初始提交，含误入库的 backend/.env 密钥）
  - `git merge-base --is-ancestor 860a131 main` → exit 1（非祖先）
- 用户明确同意后已执行：`git push --force-with-lease origin main` → `+ 860a131...c9f389d main -> main (forced update)` **成功**；`git status -sb` = `## main...origin/main`（无 ahead/behind，已同步），upstream 跟踪正常
- 遗留：`.codebuddy/memory/2026-09-13.md` 为未跟踪文件（未提交，符合"不擅自 commit"约定）
